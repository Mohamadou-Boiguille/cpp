#ifndef PMERGEME_HPP
#include <cstdlib>
#include <iostream>
#include <iterator>
#include <limits.h>
#include <list>

class PmergeMe
{
  public:
	PmergeMe();
	PmergeMe(char **inputArray);
	PmergeMe(const PmergeMe &other);
	PmergeMe &operator=(const PmergeMe &other);
	~PmergeMe();

  private:
	// functions
	// void insertionSort(std::list<int> toInsert);
	// void mergeSort(std::list<int> toMerge);
	void mergeInsertionSort(std::list<int> &toMergeInsert);
	void displayList(std::list<int> listToDisplay);
	void convertInputToList(char **inputArray);
	// variables
	std::list<int> nbList;
	// std::list<int> _leftHalf;
	// std::list<int> _rightHalf;
};
#endif // !PMERGEME_HPP
